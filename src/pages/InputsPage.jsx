// InputsPage.jsx
// The inputs page logic is currently inline in App.jsx (tab === "inputs" section)
// to share the Field, Card, Sec, and other UI atoms and the kpi/inp/upd state.
// This file is reserved for a future extraction of the full inputs page when
// those UI atoms are moved to a shared ui/ directory.

export default function InputsPage({ inp, upd, kpi }) {
  // See App.jsx inputs tab section for the full implementation.
  return null;
}
